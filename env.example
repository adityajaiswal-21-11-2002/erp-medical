# ===========================================
# ERP Medical – Environment variables
# ===========================================
# Rename this file to .env (or copy its contents into .env):
#   copy env.example .env   (Windows)
#   cp env.example .env    (macOS/Linux)

# --- Backend (required) ---
# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/erp-medical

# JWT secrets (at least one required: JWT_ACCESS_SECRET or JWT_SECRET)
JWT_ACCESS_SECRET=your-super-secret-jwt-access-key-change-in-production
JWT_SECRET=

# Optional: separate refresh token secret
JWT_REFRESH_SECRET=

# --- Backend (optional) ---
# Allowed CORS origin for the Next.js app
CLIENT_ORIGIN=http://localhost:3000
# API server port
PORT=5000
# Public app URL (for webhooks / redirects)
PUBLIC_APP_URL=http://localhost:3000

# --- Shipping: Shiprocket ---
SHIPROCKET_EMAIL=
SHIPROCKET_PASSWORD=
# SHIPROCKET_BASE_URL=https://apiv2.shiprocket.in

# --- Shipping: RapidShyp ---
RAPIDSHYP_API_KEY=
# RAPIDSHYP_BASE_URL=https://api.rapidshyp.com

# --- Shipping: default provider ---
DEFAULT_SHIPPING_PROVIDER=SHIPROCKET

# --- Razorpay (IDFC) ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# --- Frontend (Next.js) ---
# Leave NEXT_PUBLIC_API_URL unset (or empty) to use Next.js API routes — no separate backend needed.
# Set only when using the Express backend: NEXT_PUBLIC_API_URL=http://localhost:5000
# NEXT_PUBLIC_API_URL=
# Public Razorpay key (frontend only; never put RAZORPAY_KEY_SECRET in frontend)
# NEXT_PUBLIC_RAZORPAY_KEY_ID=

# --- E2E (optional) ---
# E2E_BASE_URL=http://localhost:3000
# E2E_API_URL=http://localhost:5000

# --- Test / deterministic seed ---
# For backend tests: copy backend/.env.test or set MONGODB_URI to a test DB.
# SEED_DETERMINISTIC=1 — use fixed ref codes (e.g. REF-DEMO1) and predictable data for E2E.
